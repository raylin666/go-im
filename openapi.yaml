# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ""
    version: 0.0.1
paths:
    /api/heartbeat:
        get:
            tags:
                - Heartbeat
            description: Heartbeat detection
            operationId: Heartbeat_PONE
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/v1.PONEResponse'
    /api/manager/create:
        post:
            tags:
                - Manager
            description: Manager detection
            operationId: Manager_Create
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/v1.ManagerCreateRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/v1.ManagerCreateResponse'
components:
    schemas:
        v1.ManagerCreateRequest:
            type: object
            properties:
                ident:
                    type: string
                name:
                    type: string
                status:
                    type: integer
                    format: enum
                expiredAt:
                    type: string
                    format: date-time
        v1.ManagerCreateResponse:
            type: object
            properties:
                id:
                    type: string
                ident:
                    type: string
                name:
                    type: string
                key:
                    type: string
                secret:
                    type: string
                status:
                    type: integer
                    format: enum
                expiredAt:
                    type: string
                    format: date-time
                createdAt:
                    type: string
                    format: date-time
        v1.PONEResponse:
            type: object
            properties:
                message:
                    type: string
tags:
    - name: Heartbeat
      description: The Heartbeat service definition.
    - name: Manager
      description: The Manager service definition.
